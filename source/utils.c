#include "utils.h"

/**
 * @brief       Subtracts second input argument from first.
 *
 * @param[in]   num1    First argument.
 * @param[in]   num2    Second argument.
 */
int sub (int num1, int num2)
{
    return (num1 - num2);
}

/**
 * @brief       Sums the two input arguments and returns the result.
 *
 * @param[in]   num1    First argument.
 * @param[in]   num2    Second argument.
 */
int sum (int num1, int num2)
{
    return (num1 + num2);
}

int main(void)
{
    int number1;
    int number2;
    int result;
}

//test
//test